{
  "name": "juicebox-neutralino",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "neu build --release",
    "build:all": "neu build --release --arch x64 --platform win32 && neu build --release --arch x64 --platform linux && neu build --release --arch x64 --platform darwin",
    "build:win": "neu build --release --arch x64 --platform win32",
    "build:mac": "neu build --release --arch x64 --platform darwin",
    "build:linux": "neu build --release --arch x64 --platform linux",
    "dev": "neu run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@neutralinojs/neu": "^11.7.1"
  }
}